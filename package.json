{
  "name": "mern-template",
  "version": "1.0.0",
  "description": "Simple template for the MERN stack",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "yarn run dev",
    "dev": "concurrently \"cd client && yarn run start\" \"cd server && yarn run dev\"",
    "init": "concurrently \"cd client && yarn install\" \"cd server && yarn install\""
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
